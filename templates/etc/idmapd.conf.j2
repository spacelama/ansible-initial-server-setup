[General]

Verbosity = 0
{% if deb_release | default('bullseye') == 'bullseye' %}
Pipefs-Directory = /run/rpc_pipefs
{% endif %}
# set your own domain here, if it differs from FQDN minus hostname
# Domain = localdomain

[Mapping]

Nobody-User = nobody
Nobody-Group = nogroup
