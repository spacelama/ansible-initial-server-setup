---
# https://stackoverflow.com/questions/18195142/safely-limiting-ansible-playbooks-to-a-single-machine
- hosts: '{{ target }}'
#- hosts: laptops
  become_method: su

  vars_files:
    - vars/main.yml

  roles:
    - bootstrap
